{
  "name": "loxtep-plugins-skills",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "check:mcp-config": "node scripts/check-mcp-config.mjs",
    "check:docs": "node scripts/check-docs-consistency.mjs",
    "check": "npm run check:mcp-config && npm run check:docs && npm test"
  },
  "devDependencies": {
    "fast-check": "^3.15.0",
    "vitest": "^1.6.0",
    "typescript": "^5.4.0"
  }
}
